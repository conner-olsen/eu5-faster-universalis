# vanilla: in_game/gui/map_markers.gui
# mod: in_game/gui/map_markers.gui

widget = {
	name = "port_marker"
	max_update_rate = 9999 # FUM: Throttle data updates
	allow_outside = yes
	
	button = {
		widgetanchor = center
		size = { 30 30 }
		visible = "[PortMarker.ShouldBeShown]"
		# down = "[PortMarker.HasVisibleShips]"
		datacontext = "[PortMarker]"

		icon = { # FUM: Both port icons and their drop shadow pre-composited into one
			parentanchor = center
			size = { 120% 120% }
			texture = "gfx/interface/buttons/flats/fum_port_marker.dds"
		}
		
		action_tooltip = {
			click_type = right
			click_mode = single
			cursor = "unit_movement"

			visible = "[PortMarker.CanMoveNavyHere]"
			title = "PORT_CLICK_ACTION_UNIT_TRANSPORT"
			on_action = "[PortMarker.MoveNavyHere]"
		}

		tooltipwidget = {
			ContextualTooltipType = {
				blockoverride "title_icon" {
					icon = {
						using = tooltip_title_icon_size
						texture = "gfx/interface/icons/flat_icons/geopolitics/maritime_presence.dds"
		
						glow = {
							name = "drop_shadow"
							glow_radius = 3
							color = {0.0 0.0 0.0 1.0}
							alpha = 0.3
						}
					}
				}
		
				blockoverride "title_text" {
					text = "PORT_TOOLTIP_NEW"
				}
		
				blockoverride "concept_link" {
					text = "[port|e]" 
				}
		
				blockoverride "tooltip_content" {
					TooltipTextBlock = {
						blockoverride "text" {
							text = "PORT_TOOLTIP_TEXT"
						}
					}
				}
			}
		}
	}
}
